const initialState = {
  order: {},
};
export const modalOrderReducer = (state = initialState, action) => {
  /*      switch (action.type) 
      
      default: 
           return state  */
};
